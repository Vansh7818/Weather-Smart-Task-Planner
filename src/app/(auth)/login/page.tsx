import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Sign In | Weather SmartPlanner AI",
  description: "Sign in to your Weather SmartPlanner account",
};

export default function LoginPage() {
  return <LoginForm />;
}
